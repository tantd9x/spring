### The name of our service- all service must have a name to be discovered, it MUST be unique
spring:
  application:
    name: spring-sample
  profiles:
    active: dev
  cloud:
    config:
      ### Location of our config-service; switch to eureka discovery client
      uri: http://localhost:8888
